import React from 'react'

function MenuDisplay(){
    return (
        <div>

        </div>
    )
}

export {MenuDisplay}; // converts default into named component, makes it easier to import into App.js i.e no need to right'import xxx from xxxx'